name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Build with Astro
        uses: withastro/action@v6
        with:
          # Astro 7 requires Node >= 22.12. withastro/action@v6 already defaults
          # to Node 24, which satisfies that, but pin 22 explicitly to match the
          # other workflows (setup-node uses 22) and keep the runtime consistent.
          node-version: 22
        # withastro/action installs deps, runs `astro build`, and uploads
        # the dist/ folder as the Pages artifact.

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
